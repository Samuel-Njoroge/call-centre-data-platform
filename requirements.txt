# Root environment for this repo. Install with:
#   ./venv/Scripts/python.exe -m pip install -r requirements.txt

# ingestion (local raw loader)
duckdb>=1.1.0

# ingestion (fx rate integration)
requests>=2.31.0
redshift-connector>=2.1.0

# transformation
dbt-core>=1.9.0
dbt-duckdb>=1.9.0
dbt-redshift>=1.9.0

# env var loading for dbt / scripts (.env -> process env)
python-dotenv>=1.0.0

# orchestration
dagster>=1.8.0
dagster-webserver>=1.8.0

# s3 upload
boto3>=1.34.0

# tests
pytest>=8.0.0
